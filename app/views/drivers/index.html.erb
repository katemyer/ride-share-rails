<h1>Drivers List</h1>

<ul>
  <% @drivers.each do |driver|  %>
    <li>
      ID: <%= driver[:id] %> 
      Name: <%= link_to driver.name, drivers_path(driver.id) %>
      VIN: <%= driver[:vin] %>
      Available: <%= driver[:available] %>
    </li>
  <% end %>
</ul>