<h1>Passengers List</h1>

<ul>
  <% @passengers.each do |passenger|  %>
    <li>
      ID: <%= passenger[:id] %> 
      Name: <%= link_to passenger.name, passengers_path(passenger.id) %>
      Phone: <%= passenger[:phone_num] %>
    </li>
  <% end %>
</ul>

